name: OpenRabbit
on:
  pull_request_target:
    types: [opened, reopened, edited, synchronize]

permissions:
  contents: read
  pull-requests: write

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - name: OpenRabbit
        uses: aryan6673/openrabbit@v0.2.8
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          bot_token: ${{ secrets.REVIEW_BOT_TOKEN }}   # optional
          llm_provider: openrouter
          llm_api_url: https://openrouter.ai/api/v1
          llm_api_key: ${{ secrets.LLM_API_KEY }}
          llm_model: openrouter/free
          review_mode: both
          tone_mode: balanced
